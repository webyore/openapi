{
  "name": "webyore-api-examples",
  "private": true,
  "type": "module",
  "engines": { "node": ">=22" },
  "scripts": {
    "test": "node --test test/*.test.mjs",
    "spec:check": "node scripts/sync-spec.mjs --check",
    "spec:update": "node scripts/sync-spec.mjs"
  }
}
